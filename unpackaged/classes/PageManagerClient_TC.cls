@isTestprivate class PageManagerClient_TC {       static testMethod void testConstructors01() {        PageManagerClient pm0 = new PageManagerClient();        PageManagerClient pm1 = new PageManagerClient(5);        System.assertEquals(1, pm1.pageList[0]);                }        static testMethod void testProperties01() {        // Normal Case 1        PageManagerClient pm = new PageManagerClient(10);        pm.numberOfRows = 102;        System.assertEquals(11, pm.pageList.size());        System.assertEquals(1, pm.pageList[0]);        System.assertEquals(11, pm.pageList[pm.pageList.size()-1]);        pm.page = 0;        System.assertEquals(0, pm.startIndex);        System.assertEquals(10, pm.endIndex);        pm.page = 10;        System.assertEquals(100, pm.startIndex);        System.assertEquals(102, pm.endIndex);        List<SelectOption> options = pm.pageOptions;    }        }